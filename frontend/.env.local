# frontend/.env.local

# URL de l'API Backend (à modifier quand le backend sera prêt)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# URL Socket.IO (à modifier quand le backend sera prêt)
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000

# Google OAuth Client ID (optionnel pour l'instant)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id